import ServiceDocumentation from '@site/src/components/documentation/ServiceDocumentation'
import siteConfig from '@generated/docusaurus.config'
import { generateServiceTOC } from '@site/src/components/documentation/generateServiceTOC'

# Player presence API

export const toc = [
  { value: 'Endpoints', id: 'endpoints', level: 2 },
  ...generateServiceTOC('PlayerPresenceAPIService', siteConfig)
]

The player presence API allows you to automatically track the presence of players in your game. This can be useful for a variety of use cases, such as showing a player as online or offline, setting custom statuses and friends lists.

## Endpoints

<ServiceDocumentation
  service='PlayerPresenceAPIService'
  metaDescription="With Talo's Player Presence API, you can automatically track if players are online or offline. Presence allows you to easily build social features like friends lists."
/>
